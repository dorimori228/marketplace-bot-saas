# Railway.toml - Configuration for Railway deployment
# https://docs.railway.app/deploy/config-as-code

[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements_cloud.txt"

# Deploy commands are set per-service in Railway UI so
# the web service can run gunicorn and the worker can
# run celery with different start commands.
